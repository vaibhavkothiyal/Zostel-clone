import { Route, Routes } from "react-router-dom";

export const DirectRoutes=()=>{
    return (
        <Routes>
            {/* <Route path="/" element={<Home />}></Route> */}
        </Routes>
    )
}